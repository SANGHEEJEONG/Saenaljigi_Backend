spring:
 datasource:
   url: jdbc:mysql://localhost:3306/saenaljigi?useSSL=false&serverTimezone=Asia/Seoul
   username: root
   password: sleeDY0510!!
   driver-class-name: com.mysql.cj.jdbc.Driver

   jpa:
     hibernate:
       ddl-auto: validate  # 애플리케이션 시작 시 테이블을 자동 생성
       dialect: org.hibernate.dialect.MySQLDialect  # MySQL 방언 설정
     show-sql: true  # 콘솔에 실행된 SQL을 출력
